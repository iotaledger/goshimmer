syntax = "proto3";

option go_package = "github.com/wollac/autopeering/grpc";

package grpc;

import "proto/packet.proto";

message Empty {}

service Peering {
  // UDP style send without responds
  rpc Send(proto.Packet) returns (Empty);
}
