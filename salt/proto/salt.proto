syntax = "proto3";

option go_package = "github.com/wollac/autopeering/salt/proto";

package proto;

message Salt {
    // value of the salt
    bytes value = 1;
    // expiration time of the salt
    bytes expTime = 2;
}