syntax = "proto3";

option go_package = "github.com/wollac/autopeering/transport/proto";

package proto;

import "google/protobuf/empty.proto";
import "server/proto/packet.proto";

service Discover {
  // send a packet without expecting a response
  rpc Send(proto.Packet) returns (google.protobuf.Empty);
}
